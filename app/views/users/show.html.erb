

<img class="img-responsive" src="http://cliparts.co/cliparts/BTg/rbz/BTgrbzRkc.jpg" alt="">
<div class="container">
  <div class="row">
    <div class="col-sm-4">
      <img class="img-responsive img-rounded hidden-xs" src="<%= @user.image_url %>" alt="">
      <h3><%= @user.full_name %></h3>
      <h3>Joined: <%= @user.created_at.strftime("%x") %></h3>
      <h3>Current City: <%= @user.current_city %></h3>
    </div>
    <div class="col-sm-8">
      <% @questions.reverse_each do |post| %>
        <div class="questions">
          <h2><%= link_to questions.title, user_question_path(question.user, question) %></h2>
          <p><%= question.content %></p>
        </div>
      <% end %>
    </div>
  </div>
</div>